<div class="row">
    {{#each todos}}
    <div class="col-4">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">{{title}}</h5>
                <p class="card-text">{{description}}</p>
                <button type="button" class="btn btn-warning"><a href="#">Detail</a></button>
            </div>
        </div>
    </div>
    {{/each}}
</div>
